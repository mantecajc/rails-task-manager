<h1>My tasks</h1>
<% @tasks.each do |task| %>
  <p>
	<% if task.completed?  %>
	  Done!
	<% else %>
	  Waiting...
	<% end %>
	<%= link_to task.title, task_path(task) %> 
  </p>
<% end %>

<hr>
<%= link_to "Add a new task", tasks_new_path %>


